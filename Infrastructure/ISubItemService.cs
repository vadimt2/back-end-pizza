using Common;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace Infrastructure
{
    public interface ISubItemService
    {
        //Task<int> Delete(int id);
        //Task<IEnumerable<SubItem>> Get();
        //Task<SubItem> Get(int id);
        //Task<SubItem> Inseret(SubItem subCat);
        //Task<SubItem> Update(int id, SubItem subCat);
    }
}